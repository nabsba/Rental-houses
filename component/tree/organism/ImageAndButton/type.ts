import TImageNext from '../../../Common/types/next';
import { TButtonOriginal } from '../../molecule';

type TImageAndButton = {
  imageAsComponent: TImageNext;
  button: TButtonOriginal;
  route: string;
};
export default TImageAndButton;
